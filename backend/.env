PG_DB=auto_repair
PG_HOST=localhost
PG_USER=auto_repair
PG_PASSWORD=auto_repair_pass
PG_PORT=54321
MIGRATION_DIR=./migrations

PG_DSN="host=localhost port=$PG_PORT dbname=$PG_DB user=$PG_USER password=$PG_PASSWORD sslmode=disable"

SERVICE_HOST=localhost
SERVICE_PORT=8080

REMOTELY_CONNECT="psql -h <REMOTE HOST> -p <REMOTE PORT> -U <DB_USER> <DB_NAME>"